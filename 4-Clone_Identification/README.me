The only step for determining clones is to use the R script in the scripts/ folder here. The included vcf file is used as input. Note that this step should be done after the SNP calling (step 5), but before determining outliers. 
